{
  "id": "fin-evolution-engine",
  "name": "Evolution Engine",
  "description": "Self-evolving strategy engine with RDAVD cycle, survival tiers, mutation risk gates, and constitution enforcer",
  "kind": "financial",
  "version": "2026.2.28",
  "configSchema": {
    "type": "object",
    "properties": {
      "evolution": {
        "type": "object",
        "properties": {
          "evaluationInterval": { "type": "string", "default": "monthly" },
          "cullRatio": { "type": "number", "default": 20 },
          "mutationProbability": { "type": "number", "default": 0.3 },
          "minStrategies": { "type": "number", "default": 3 }
        }
      }
    }
  }
}
