{
  "id": "fin-monitoring",
  "name": "Financial Monitoring",
  "description": "Proactive financial monitoring: price alerts, portfolio health checks, scheduled reports",
  "kind": "financial",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "autoEvaluate": {
        "type": "boolean",
        "default": true,
        "description": "Enable periodic monitoring checks for active alerts"
      },
      "runOnStart": {
        "type": "boolean",
        "default": true,
        "description": "Run one monitoring check immediately when the service starts"
      },
      "pollIntervalMs": {
        "type": "number",
        "default": 300000,
        "description": "Monitoring check interval in milliseconds (minimum 10000)"
      }
    }
  }
}
