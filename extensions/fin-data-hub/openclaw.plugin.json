{
  "id": "fin-data-hub",
  "name": "openFinclaw DataHub",
  "description": "Financial data bridge to Tushare proxy — A-shares, HK stocks, US equities, Macro, Derivatives, Crypto. Set TUSHARE_PROXY_API_KEY for real-time data.",
  "kind": "financial",
  "version": "2026.3.1",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "tushareProxyUrl": {
        "type": "string",
        "default": "http://43.134.187.48:7088",
        "description": "Tushare proxy base URL"
      },
      "tushareApiKey": {
        "type": "string",
        "description": "API key for Tushare proxy (X-Api-Key header)"
      },
      "apiKey": {
        "type": "string",
        "description": "Legacy: alias for tushareApiKey (backward compatible)"
      },
      "endpoint": {
        "type": "string",
        "description": "Legacy: alias for tushareProxyUrl (backward compatible)"
      },
      "mode": {
        "type": "string",
        "enum": ["live", "stub"],
        "default": "live",
        "description": "live = real data via Tushare proxy; stub = sample data for testing"
      },
      "requestTimeoutMs": {
        "type": "number",
        "default": 30000,
        "minimum": 1000,
        "maximum": 120000,
        "description": "HTTP request timeout in milliseconds"
      }
    }
  },
  "uiHints": {
    "tushareProxyUrl": {
      "label": "Tushare Proxy URL",
      "advanced": true,
      "placeholder": "http://43.134.187.48:7088",
      "help": "Base URL for the Tushare proxy server. Default works out-of-the-box."
    },
    "tushareApiKey": {
      "label": "Tushare API Key",
      "sensitive": true,
      "placeholder": "your-tushare-proxy-api-key",
      "help": "API key for the Tushare proxy. Also readable from TUSHARE_PROXY_API_KEY env var."
    },
    "mode": {
      "label": "Mode",
      "advanced": true,
      "help": "live = real financial data via Tushare proxy; stub = offline sample data for development"
    },
    "requestTimeoutMs": {
      "label": "Request Timeout (ms)",
      "advanced": true,
      "placeholder": "30000",
      "help": "Timeout for each API request (default 30s, max 120s)"
    }
  }
}
